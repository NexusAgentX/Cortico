<!-- Owner: src/providers/base.ts, src/providers/openai-responses-compat/native.ts -->
# Providers

`base.ts` defines the injected Provider interface. `openai-responses-compat/` implements native
Responses requests. `transport/` handles HTTP, SSE, request replay, response assembly and token meters.
See [Provider contracts](../../docs/providers.md).
