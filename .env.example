# 仓库根 .env 的样板。复制成 .env 再改(.env 已在 .gitignore 里)。
#
# 这个文件只回答一个问题:**这台机器上的部署装在哪**。它不是密钥文件——
# API key 之类归部署单位自己,写在 <部署根>/<部署名>/.env 或进程环境里,
# 仓库根的 .env 里写密钥是读不到的(见 src/deploy.ts 的 secret())。
#
# 没有这个文件也能跑:不设 CORTICO_HOME 时部署根就是主仓库的 deployments/。

# CORTICO_HOME —— 部署根,所有部署目录的父目录。
#
# 一份部署 = 一个 <CORTICO_HOME>/<名字>/ 目录:deployment.json 说它用哪个代码包,
# 旁边是 config.json、.env、data/、persona|workspace/ 这些"换一台机器就不一样"的东西。
#
# 解析顺序:进程环境的 CORTICO_HOME > 这个文件里的 CORTICO_HOME > 默认值。
# 默认值 = <主仓库根>/deployments。
# 写相对路径按**主仓库根**解析(不是当前 git worktree),写绝对路径原样采用;
# 值里有空格就加引号。
#
# 什么时候要改:
#   - 部署数据要放在仓库外(换机器只搬这一个目录、或它在另一块盘/网络盘上);
#   - 同一份代码要跑好几套互不相干的部署(测试的一套、直播的一套);
#   - 仓库整个是只读的(容器镜像、CI 检出),数据必须落在别处。
#
# 不用改的情况:用 git worktree 开分支干活。worktree 共享主仓库,部署根也跟着
# 指回主仓库那一份,不会各起一套数据。
#
#CORTICO_HOME=D:\cortico-deployments
#CORTICO_HOME=../cortico-deployments
